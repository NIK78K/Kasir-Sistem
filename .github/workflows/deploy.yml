name: Deploy to Bizznet VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_mysql, dom, filter, gd, json, zip
          coverage: none

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install NPM dependencies
        run: npm ci

      - name: Build assets
        run: npm run build

      - name: Create deployment archive
        run: |
          # 1. Buat arsip di folder /tmp (di luar folder project) agar tidak error 'file changed'
          tar -czf /tmp/deployment.tar.gz \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='node_modules' \
            --exclude='tests' \
            --exclude='.env' \
            --exclude='storage/logs/*' \
            --exclude='storage/framework/cache/*' \
            --exclude='storage/framework/sessions/*' \
            --exclude='storage/framework/views/*' \
            .
          
          # 2. Pindahkan file yang sudah jadi kembali ke folder project untuk di-upload
          mv /tmp/deployment.tar.gz ./deployment.tar.gz

      - name: Deploy to VPS (Upload)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "deployment.tar.gz"
          target: "/tmp/"

      - name: Execute deployment script (SSH)
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            # Tentukan path project
            PROJECT_PATH="/var/www/Kasir-Sistem"
            cd $PROJECT_PATH
            
            # 1. Backup versi lama (Simpan di folder home user agar aman)
            timestamp=$(date +%Y%m%d_%H%M%S)
            BACKUP_DIR="$HOME/backups/kasir-sistem"
            mkdir -p $BACKUP_DIR
            
            echo "📦 Membuat Backup ke $BACKUP_DIR..."
            tar -czf "$BACKUP_DIR/backup_$timestamp.tar.gz" . --exclude=node_modules 2>/dev/null || true
            
            # 2. Ekstrak versi baru dari GitHub
            echo "📂 Mengekstrak File Baru..."
            tar -xzf /tmp/deployment.tar.gz -C .
            rm /tmp/deployment.tar.gz
            
            # 3. Atur Permission
            echo "wm Mengatur Izin Akses..."
            chmod -R 775 storage bootstrap/cache
            
            # 4. Update Database & Laravel
            echo "🚀 Menjalankan Migrasi & Cache..."
            php artisan migrate --force
            
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            
            echo "✅ Deployment Selesai! Versi $timestamp tersimpan."

      - name: Notify deployment status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Deployment successful!"
          else
            echo "❌ Deployment failed!"
            exit 1
          fi